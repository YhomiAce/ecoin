<!-- header -->
<%- include("../partials/header_admin") %>
<%- include("../partials/sidebar") %>

<!-- Main Content -->
<div class="main-content">
    <%- include("../partials/alerts") %>
    <section class="section">
        <div class="section-body">
            <div class="row">
                <div class="col-12">
                    <div class="card">
                        <div class="card-header">
                            <h4>Unapproved Withdrawals</h4>
                        </div>
                        <div class="card-body">
                            <div class="table-responsive">
                                <table class="table table-striped" id="table-1">
                                    <thead>
                                        <tr>
                                            <th class="text-center">#</th>
                                            <th>Name</th>
                                            <th>Email</th>
                                            <th>Amount</th>
                                           
                                            
                                            <th>Wallet Name</th>
                                            <th>Wallet Address</th>
                                            <th>Date</th>
                                            <th>Action</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <%  for (let i = 0; i < withdrawals.length; i++ ) { %>
                                        <tr>
                                            <td><%= i + 1 %></td>
                                            <td><%= withdrawals[i].user.name%></td>
                                            <td><%= withdrawals[i].user.email %></td>
                                            <td>$<%= withdrawals[i].amount %>.00</td>
                                           

                                            <td><%= withdrawals[i].wallet_name %></td>
                                            <td><%= withdrawals[i].wallet_address %></td>
                                            <td><%= withdrawals[i].createdAt %></td>
                                            <td>
                                                <a href="javascript:void(0);" class="btn btn-warning btn-pill"
                                                    onclick="event.preventDefault();
								                        document.getElementById('approve-withdrawal').submit();" style="color: aliceblue;">
                                                    Approve
                                                </a>
                                                <form id="approve-withdrawal" action="/approve-withdrawal" method="POST"
                                                    style="display: none;">
                                                    <input type="hidden" name="id" value="<%= withdrawals[i].id %>">
                                                    <input type="hidden" name="amount" value="<%= withdrawals[i].amount %>">
                                                    
                                                </form>
                                            </td>
                                        </tr>
                                        <% } %>
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
</div>
<%- include("../partials/footer2") %>